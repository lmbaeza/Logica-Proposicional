void main() {
    p : 2*(6*-1) == 12;
    escribir(p);

    x:=10;
    escribir(x);

    p1 : [(3*1 + 4*2)]*2 <> x;
    escribir(p1);

    p2 : [34] ≠ 34;
    escribir(p2);

    y := 5*x-100;
    escribir(y);
}